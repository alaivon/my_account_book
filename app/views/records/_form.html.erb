<%= form_for record do |f| %>
	<% if @record.errors.any? %>
    	<div id="error_explanation">
      <h5><%= pluralize(record.errors.count, "error") %> prohibited this product from being saved:</h5>
      <ul>
      	<% record.errors.full_messages.each do |message| %>
        	<li><%= message %></li>
      	<% end %>
      </ul>
    </div>
  <% end %>


<div class="form-group">
	<%= f.label "Types" %> <br>
	<%= f.collection_select :catagory_id, Catagory.all, :id, :kind, { promt: "Choose a Type" } %> <br>
</div>
<div class="form-group">

  <%= f.label :period %>	<br>
  <%= f.date_select :period %> <br>
</div>
<div class="form-group">

	<%= f.label :amount %> <br>
	<%= f.number_field :amount %> <br>
</div>
<div class="form-group">
	<%= f.label :info %> <br>
	<%= f.text_field :info %> <br>
</div>
  <br>
	<%= f.submit class: "btn btn-default btn-sm" %>
<% end %>