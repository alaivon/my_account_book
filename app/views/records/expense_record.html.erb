<div class="container-fluid">
	<div class="row">
		<div class="col-md-6 col-md-offset-3">
			<table class="table table-hover table-bordered">
				<h4>Expense</h4>
				<div class="show_total">
					<p class="total">Total <span id="total"><%= render_total_amount(@expense_record) %></span> from <span id="early_date"><%= render_earliest_date(Record, 2) %></span></p>
				</div>
				<%= render 'table', record: @expense_record, id: 2 %>		
			</table>
		</div>
	</div>
</div>
