<div class="container-fluid">
	<div class="row">
		<div class="col-md-6 col-md-offset-3">
			<table class="table table-hover table-bordered">
				<h4>Revenue</h4>
				<p class="total">Total <span id="total"><%= render_total_amount(@revenue_record) %></span> from <span id="early_date"><%= render_earliest_date(Record, 1) %></span></p>

				<%= render 'table', record: @revenue_record %>		
			</table>
		</div>
	</div>
</div>